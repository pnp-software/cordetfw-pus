/**
 * \file
 *
 * Interface for accessing fields in packets of service "Tst".
 *
 * \note This file was generated on 2018-06-22 17:45:58
 * \author Automatically Generated by CORDET Editor
 * \copyright P&P Software GmbH
 */
#ifndef CRPSPKTTST_H_
#define CRPSPKTTST_H_

#include "CrPsTypes.h"

#include "CrPsPkt.h"
/**
 * Structure for TstAreYouAliveCmd
 */
typedef struct __attribute__((packed)) _TstAreYouAliveCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

} TstAreYouAliveCmd_t ;

/**
 * Structure for TstAreYouAliveRep
 */
typedef struct __attribute__((packed)) _TstAreYouAliveRep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

} TstAreYouAliveRep_t ;

/**
 * Structure for TstConnectCmd
 */
typedef struct __attribute__((packed)) _TstConnectCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Identifier of the application with which the connection test must be
    * done
    */
   CrFwDestSrc_t AppId;
   
} TstConnectCmd_t ;

/**
 * Structure for TstConnectRep
 */
typedef struct __attribute__((packed)) _TstConnectRep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Identifier of application with which the connection test was done
    */
   CrFwDestSrc_t AppId;
   
} TstConnectRep_t ;

/**
 * Get "AppId" from "TstConnectCmd" packet.
 * \param p Pointer to the packet.
 * \return Value of "AppId".
 */
static inline CrFwDestSrc_t getTstConnectCmdAppId(FwSmDesc_t cmd)
{
   CrFwPckt_t p;
   CrFwCmpData_t* cmpData;
   CrFwInCmdData_t* inCmdData;

   cmpData = (CrFwCmpData_t*) FwSmGetData(cmd);
   inCmdData = (CrFwInCmdData_t*) (cmpData->cmpSpecificData);
   p = inCmdData->pckt;

   TstConnectCmd_t* t;
   t = (TstConnectCmd_t*)p;
   return t->AppId;
}

/**
 * Set "AppId" in "TstConnectCmd" packet.
 * \param p Pointer to the packet.
 * \param AppId Value of "AppId" to be set in packet.
 */
static inline void setTstConnectCmdAppId(FwSmDesc_t cmd, CrFwDestSrc_t AppId)
{
   CrFwPckt_t p;
   CrFwCmpData_t* cmpData;
   CrFwInCmdData_t* inCmdData;

   cmpData = (CrFwCmpData_t*) FwSmGetData(cmd);
   inCmdData = (CrFwInCmdData_t*) (cmpData->cmpSpecificData);
   p = inCmdData->pckt;

   TstConnectCmd_t* t;
   t = (TstConnectCmd_t*)p;
   t->AppId = AppId;
}

/**
 * Get "AppId" from "TstConnectRep" packet.
 * \param p Pointer to the packet.
 * \return Value of "AppId".
 */
static inline CrFwDestSrc_t getTstConnectRepAppId(void* p)
{
   TstConnectRep_t* t;
   t = (TstConnectRep_t*)p;
   return t->AppId;
}

/**
 * Set "AppId" in "TstConnectRep" packet.
 * \param p Pointer to the packet.
 * \param AppId Value of "AppId" to be set in packet.
 */
static inline void setTstConnectRepAppId(void* p, CrFwDestSrc_t AppId)
{
   TstConnectRep_t* t;
   t = (TstConnectRep_t*)p;
   t->AppId = AppId;
}


/*----------------------------------------------------------------------------*/
#endif /* CRPSPKTTST_H */
