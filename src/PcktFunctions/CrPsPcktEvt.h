/**
 * @file
 * @ingroup gen_pck
 *
 * Interface for accessing fields in packets of service "Evt".
 *
 * @note This file was generated on 2018-07-10 22:24:46
 * @author Automatically Generated by CORDET Editor
 * @copyright P&P Software GmbH
 */
#ifndef CRPSPCKTEVT_H_
#define CRPSPCKTEVT_H_

#include "CrPsTypes.h"

#include "CrPsPckt.h"
/**
 * Structure for EvtRep1
 */
typedef struct __attribute__((packed)) _EvtRep1_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Event Identifier
    */
   CrPsEvtId_t EventId;
   
} EvtRep1_t ;

/**
 * Structure for EvtRep2
 */
typedef struct __attribute__((packed)) _EvtRep2_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Event Identifier
    */
   CrPsEvtId_t EventId;
   
} EvtRep2_t ;

/**
 * Structure for EvtRep3
 */
typedef struct __attribute__((packed)) _EvtRep3_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Event Identifier
    */
   CrPsEvtId_t EventId;
   
} EvtRep3_t ;

/**
 * Structure for EvtRep4
 */
typedef struct __attribute__((packed)) _EvtRep4_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Event Identifier
    */
   CrPsEvtId_t EventId;
   
} EvtRep4_t ;

/**
 * Structure for EvtEnbId
 */
typedef struct __attribute__((packed)) _EvtEnbId_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * The number of event identifiers to be enabled
    */
   CrPsNEvtId_t N;
   
   struct __attribute__((packed)) {
      /**
       * Event identifier to be enabled
       */
      CrPsEvtId_t EventId;
      
   } N_[0];
} EvtEnbId_t ;

/**
 * Structure for EvtDisIdCmd
 */
typedef struct __attribute__((packed)) _EvtDisIdCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * The number of event identifiers to be disabled
    */
   CrPsNEvtId_t N;
   
   struct __attribute__((packed)) {
      /**
       * Event identifier to be disabled
       */
      CrPsEvtId_t EventId;
      
   } N_[0];
} EvtDisIdCmd_t ;

/**
 * Structure for EvtRepDisIdCmd
 */
typedef struct __attribute__((packed)) _EvtRepDisIdCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

} EvtRepDisIdCmd_t ;

/**
 * Structure for EvtDisIdRep
 */
typedef struct __attribute__((packed)) _EvtDisIdRep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * The number of disabled event identifiers
    */
   CrPsNEvtId_t N;
   
   struct __attribute__((packed)) {
      /**
       * Event Identifier
       */
      CrPsEvtId_t EventId;
      
   } N_[0];
} EvtDisIdRep_t ;

/**
 * Get "EventId" from "EvtRep1" packet.
 * @param p Pointer to the packet.
 * @return Value of "EventId".
 */
static inline CrPsEvtId_t getEvtRep1EventId(void* p)
{
   EvtRep1_t* t;
   t = (EvtRep1_t*)p;
   return t->EventId;
}

/**
 * Set "EventId" in "EvtRep1" packet.
 * @param p Pointer to the packet.
 * @param EventId Value of "EventId" to be set in packet.
 */
static inline void setEvtRep1EventId(void* p, CrPsEvtId_t EventId)
{
   EvtRep1_t* t;
   t = (EvtRep1_t*)p;
   t->EventId = EventId;
}

/**
 * Get "EventId" from "EvtRep2" packet.
 * @param p Pointer to the packet.
 * @return Value of "EventId".
 */
static inline CrPsEvtId_t getEvtRep2EventId(void* p)
{
   EvtRep2_t* t;
   t = (EvtRep2_t*)p;
   return t->EventId;
}

/**
 * Set "EventId" in "EvtRep2" packet.
 * @param p Pointer to the packet.
 * @param EventId Value of "EventId" to be set in packet.
 */
static inline void setEvtRep2EventId(void* p, CrPsEvtId_t EventId)
{
   EvtRep2_t* t;
   t = (EvtRep2_t*)p;
   t->EventId = EventId;
}

/**
 * Get "EventId" from "EvtRep3" packet.
 * @param p Pointer to the packet.
 * @return Value of "EventId".
 */
static inline CrPsEvtId_t getEvtRep3EventId(void* p)
{
   EvtRep3_t* t;
   t = (EvtRep3_t*)p;
   return t->EventId;
}

/**
 * Set "EventId" in "EvtRep3" packet.
 * @param p Pointer to the packet.
 * @param EventId Value of "EventId" to be set in packet.
 */
static inline void setEvtRep3EventId(void* p, CrPsEvtId_t EventId)
{
   EvtRep3_t* t;
   t = (EvtRep3_t*)p;
   t->EventId = EventId;
}

/**
 * Get "EventId" from "EvtRep4" packet.
 * @param p Pointer to the packet.
 * @return Value of "EventId".
 */
static inline CrPsEvtId_t getEvtRep4EventId(void* p)
{
   EvtRep4_t* t;
   t = (EvtRep4_t*)p;
   return t->EventId;
}

/**
 * Set "EventId" in "EvtRep4" packet.
 * @param p Pointer to the packet.
 * @param EventId Value of "EventId" to be set in packet.
 */
static inline void setEvtRep4EventId(void* p, CrPsEvtId_t EventId)
{
   EvtRep4_t* t;
   t = (EvtRep4_t*)p;
   t->EventId = EventId;
}

/**
 * Get "N" from "EvtEnbId" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNEvtId_t getEvtEnbIdN(void* p)
{
   EvtEnbId_t* t;
   t = (EvtEnbId_t*)p;
   return t->N;
}

/**
 * Set "N" in "EvtEnbId" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setEvtEnbIdN(void* p, CrPsNEvtId_t N)
{
   EvtEnbId_t* t;
   t = (EvtEnbId_t*)p;
   t->N = N;
}

/**
 * Get pointer to "EventId" array from "EvtEnbId" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the EventId array.
 */
static inline CrPsEvtId_t* getEvtEnbIdEventIdArray(void* p)
{
   EvtEnbId_t* t;
   t = (EvtEnbId_t*)p;
   return &t->N_[0].EventId;
}

/**
 * Get "EventId" array from "EvtEnbId" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readEvtEnbIdEventIdArray(void* p, void* dest)
{
   EvtEnbId_t* t;
   t = (EvtEnbId_t*)p;
   memcpy(dest, &t->N_[0].EventId, t->N*sizeof(t->N_[0].EventId));
}

/**
 * Set "EventId" array in "EvtEnbId" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeEvtEnbIdEventIdArray(void* p, const void* src)
{
   EvtEnbId_t* t;
   t = (EvtEnbId_t*)p;
   memcpy(&t->N_[0].EventId, src, t->N*sizeof(t->N_[0].EventId));
}

/**
 * Get "EventId" from "EvtEnbId" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "EventId".
 */
static inline CrPsEvtId_t getEvtEnbIdEventId(void* p, CrPsNEvtId_t N)
{
   EvtEnbId_t* t;
   t = (EvtEnbId_t*)p;
   return t->N_[N].EventId;
}

/**
 * Set "EventId" in "EvtEnbId" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param EventId Value of "EventId" to be set in packet.
 */
static inline void setEvtEnbIdEventId(void* p, CrPsNEvtId_t N, CrPsEvtId_t EventId)
{
   EvtEnbId_t* t;
   t = (EvtEnbId_t*)p;
   t->N_[N].EventId = EventId;
}

/**
 * Get "N" from "EvtDisIdCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNEvtId_t getEvtDisIdCmdN(void* p)
{
   EvtDisIdCmd_t* t;
   t = (EvtDisIdCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "EvtDisIdCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setEvtDisIdCmdN(void* p, CrPsNEvtId_t N)
{
   EvtDisIdCmd_t* t;
   t = (EvtDisIdCmd_t*)p;
   t->N = N;
}

/**
 * Get pointer to "EventId" array from "EvtDisIdCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the EventId array.
 */
static inline CrPsEvtId_t* getEvtDisIdCmdEventIdArray(void* p)
{
   EvtDisIdCmd_t* t;
   t = (EvtDisIdCmd_t*)p;
   return &t->N_[0].EventId;
}

/**
 * Get "EventId" array from "EvtDisIdCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readEvtDisIdCmdEventIdArray(void* p, void* dest)
{
   EvtDisIdCmd_t* t;
   t = (EvtDisIdCmd_t*)p;
   memcpy(dest, &t->N_[0].EventId, t->N*sizeof(t->N_[0].EventId));
}

/**
 * Set "EventId" array in "EvtDisIdCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeEvtDisIdCmdEventIdArray(void* p, const void* src)
{
   EvtDisIdCmd_t* t;
   t = (EvtDisIdCmd_t*)p;
   memcpy(&t->N_[0].EventId, src, t->N*sizeof(t->N_[0].EventId));
}

/**
 * Get "EventId" from "EvtDisIdCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "EventId".
 */
static inline CrPsEvtId_t getEvtDisIdCmdEventId(void* p, CrPsNEvtId_t N)
{
   EvtDisIdCmd_t* t;
   t = (EvtDisIdCmd_t*)p;
   return t->N_[N].EventId;
}

/**
 * Set "EventId" in "EvtDisIdCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param EventId Value of "EventId" to be set in packet.
 */
static inline void setEvtDisIdCmdEventId(void* p, CrPsNEvtId_t N, CrPsEvtId_t EventId)
{
   EvtDisIdCmd_t* t;
   t = (EvtDisIdCmd_t*)p;
   t->N_[N].EventId = EventId;
}

/**
 * Get "N" from "EvtDisIdRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNEvtId_t getEvtDisIdRepN(void* p)
{
   EvtDisIdRep_t* t;
   t = (EvtDisIdRep_t*)p;
   return t->N;
}

/**
 * Set "N" in "EvtDisIdRep" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setEvtDisIdRepN(void* p, CrPsNEvtId_t N)
{
   EvtDisIdRep_t* t;
   t = (EvtDisIdRep_t*)p;
   t->N = N;
}

/**
 * Get pointer to "EventId" array from "EvtDisIdRep" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the EventId array.
 */
static inline CrPsEvtId_t* getEvtDisIdRepEventIdArray(void* p)
{
   EvtDisIdRep_t* t;
   t = (EvtDisIdRep_t*)p;
   return &t->N_[0].EventId;
}

/**
 * Get "EventId" array from "EvtDisIdRep" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readEvtDisIdRepEventIdArray(void* p, void* dest)
{
   EvtDisIdRep_t* t;
   t = (EvtDisIdRep_t*)p;
   memcpy(dest, &t->N_[0].EventId, t->N*sizeof(t->N_[0].EventId));
}

/**
 * Set "EventId" array in "EvtDisIdRep" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeEvtDisIdRepEventIdArray(void* p, const void* src)
{
   EvtDisIdRep_t* t;
   t = (EvtDisIdRep_t*)p;
   memcpy(&t->N_[0].EventId, src, t->N*sizeof(t->N_[0].EventId));
}

/**
 * Get "EventId" from "EvtDisIdRep" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "EventId".
 */
static inline CrPsEvtId_t getEvtDisIdRepEventId(void* p, CrPsNEvtId_t N)
{
   EvtDisIdRep_t* t;
   t = (EvtDisIdRep_t*)p;
   return t->N_[N].EventId;
}

/**
 * Set "EventId" in "EvtDisIdRep" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param EventId Value of "EventId" to be set in packet.
 */
static inline void setEvtDisIdRepEventId(void* p, CrPsNEvtId_t N, CrPsEvtId_t EventId)
{
   EvtDisIdRep_t* t;
   t = (EvtDisIdRep_t*)p;
   t->N_[N].EventId = EventId;
}


/*----------------------------------------------------------------------------*/
#endif /* CRPSPCKTEVT_H */
