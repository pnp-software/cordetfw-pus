/**
 * @file
 * @ingroup gen_pck
 *
 * Interface for accessing fields in packets of service "Lpt".
 *
 * @note This file was generated on 2018-09-30 23:01:15
 * @author Automatically Generated by CORDET Editor
 * @copyright P&P Software GmbH
 */
#ifndef CRPSPCKTLPT_H_
#define CRPSPCKTLPT_H_

#include "CrPsTypes.h"

#include "CrPsPckt.h"
/**
 * Structure for DownFirstRep
 */
typedef struct __attribute__((packed)) _DownFirstRep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Large Message Trans. Identifier
    */
   CrPsTransId_t Transid;
   
   /**
    * Part Sequence Number
    */
   CrPsPartSeqNmb_t PartSeqNmb;
   
   /**
    * Down-transfer data (repetition value to be set to fill the packet)
    */
   CrPsSixteenBit_t Part;
   
} DownFirstRep_t ;

/**
 * Structure for DownInterRep
 */
typedef struct __attribute__((packed)) _DownInterRep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Large Message Trans. Identifier
    */
   CrPsTransId_t Transid;
   
   /**
    * Part Sequence Number
    */
   CrPsPartSeqNmb_t PartSeqNmb;
   
   /**
    * Down-transfer data (repetition value to be set to fill the packet)
    */
   CrPsSixteenBit_t Part;
   
} DownInterRep_t ;

/**
 * Structure for DownLastRep
 */
typedef struct __attribute__((packed)) _DownLastRep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Large Message Trans. Identifier
    */
   CrPsTransId_t Transid;
   
   /**
    * Part Sequence Number
    */
   CrPsPartSeqNmb_t PartSeqNmb;
   
   /**
    * Down-transfer data (repetition value is set dynamically by the host
    * application based on the amount of data to be down-transferred)
    */
   CrPsSixteenBit_t Part;
   
} DownLastRep_t ;

/**
 * Structure for UpFirstCmd
 */
typedef struct __attribute__((packed)) _UpFirstCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Large Message Trans. Identifier
    */
   CrPsTransId_t Transid;
   
   /**
    * Part Sequence Number
    */
   CrPsPartSeqNmb_t PartSeqNmb;
   
   /**
    * Up-transfer data (repetition value to be set to fill the packet)
    */
   CrPsSixteenBit_t Part;
   
} UpFirstCmd_t ;

/**
 * Structure for UpInterCmd
 */
typedef struct __attribute__((packed)) _UpInterCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Large Message Trans. Identifier
    */
   CrPsTransId_t Transid;
   
   /**
    * Part Sequence Number
    */
   CrPsPartSeqNmb_t PartSeqNmb;
   
   /**
    * Up-transfer data (repetition value to be set to fill the packet)
    */
   CrPsSixteenBit_t Part;
   
} UpInterCmd_t ;

/**
 * Structure for UpLastCmd
 */
typedef struct __attribute__((packed)) _UpLastCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Large Message Trans. Identifier
    */
   CrPsTransId_t Transid;
   
   /**
    * Part Sequence Number
    */
   CrPsPartSeqNmb_t PartSeqNmb;
   
   /**
    * Up-transfer data (repetition value is set dynamically)
    */
   CrPsSixteenBit_t Part;
   
} UpLastCmd_t ;

/**
 * Structure for UpAbortRep
 */
typedef struct __attribute__((packed)) _UpAbortRep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Large Message Trans. Identifier
    */
   CrPsTransId_t Transid;
   
   /**
    * Transfer Failure Reason
    */
   CrPsFailReason_t FailReason;
   
} UpAbortRep_t ;

/**
 * Structure for StartDownCmd
 */
typedef struct __attribute__((packed)) _StartDownCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Large Message Trans. Identifier
    */
   CrPsTransId_t Transid;
   
} StartDownCmd_t ;

/**
 * Structure for AbortDownCmd
 */
typedef struct __attribute__((packed)) _AbortDownCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Large Message Trans. Identifier
    */
   CrPsTransId_t Transid;
   
} AbortDownCmd_t ;

/**
 * Get "Transid" from "DownFirstRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "Transid".
 */
static inline CrPsTransId_t getLptDownFirstRepTransid(void* p) {
   DownFirstRep_t* t;
   t = (DownFirstRep_t*)p;
   return __builtin_bswap16(t->Transid);
}

/**
 * Set "Transid" in "DownFirstRep" packet.
 * @param p Pointer to the packet.
 * @param Transid Value of "Transid" to be set in packet.
 */
static inline void setLptDownFirstRepTransid(void* p, CrPsTransId_t Transid) {
   DownFirstRep_t* t;
   t = (DownFirstRep_t*)p;
   t->Transid = __builtin_bswap16(Transid);
}

/**
 * Get "PartSeqNmb" from "DownFirstRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "PartSeqNmb".
 */
static inline CrPsPartSeqNmb_t getLptDownFirstRepPartSeqNmb(void* p) {
   DownFirstRep_t* t;
   t = (DownFirstRep_t*)p;
   return __builtin_bswap16(t->PartSeqNmb);
}

/**
 * Set "PartSeqNmb" in "DownFirstRep" packet.
 * @param p Pointer to the packet.
 * @param PartSeqNmb Value of "PartSeqNmb" to be set in packet.
 */
static inline void setLptDownFirstRepPartSeqNmb(void* p, CrPsPartSeqNmb_t PartSeqNmb) {
   DownFirstRep_t* t;
   t = (DownFirstRep_t*)p;
   t->PartSeqNmb = __builtin_bswap16(PartSeqNmb);
}

/**
 * Get "Part" from "DownFirstRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "Part".
 */
static inline CrPsSixteenBit_t getLptDownFirstRepPart(void* p) {
   DownFirstRep_t* t;
   t = (DownFirstRep_t*)p;
   return __builtin_bswap16(t->Part);
}

/**
 * Set "Part" in "DownFirstRep" packet.
 * @param p Pointer to the packet.
 * @param Part Value of "Part" to be set in packet.
 */
static inline void setLptDownFirstRepPart(void* p, CrPsSixteenBit_t Part) {
   DownFirstRep_t* t;
   t = (DownFirstRep_t*)p;
   t->Part = __builtin_bswap16(Part);
}

/**
 * Get "Transid" from "DownInterRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "Transid".
 */
static inline CrPsTransId_t getLptDownInterRepTransid(void* p) {
   DownInterRep_t* t;
   t = (DownInterRep_t*)p;
   return __builtin_bswap16(t->Transid);
}

/**
 * Set "Transid" in "DownInterRep" packet.
 * @param p Pointer to the packet.
 * @param Transid Value of "Transid" to be set in packet.
 */
static inline void setLptDownInterRepTransid(void* p, CrPsTransId_t Transid) {
   DownInterRep_t* t;
   t = (DownInterRep_t*)p;
   t->Transid = __builtin_bswap16(Transid);
}

/**
 * Get "PartSeqNmb" from "DownInterRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "PartSeqNmb".
 */
static inline CrPsPartSeqNmb_t getLptDownInterRepPartSeqNmb(void* p) {
   DownInterRep_t* t;
   t = (DownInterRep_t*)p;
   return __builtin_bswap16(t->PartSeqNmb);
}

/**
 * Set "PartSeqNmb" in "DownInterRep" packet.
 * @param p Pointer to the packet.
 * @param PartSeqNmb Value of "PartSeqNmb" to be set in packet.
 */
static inline void setLptDownInterRepPartSeqNmb(void* p, CrPsPartSeqNmb_t PartSeqNmb) {
   DownInterRep_t* t;
   t = (DownInterRep_t*)p;
   t->PartSeqNmb = __builtin_bswap16(PartSeqNmb);
}

/**
 * Get "Part" from "DownInterRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "Part".
 */
static inline CrPsSixteenBit_t getLptDownInterRepPart(void* p) {
   DownInterRep_t* t;
   t = (DownInterRep_t*)p;
   return __builtin_bswap16(t->Part);
}

/**
 * Set "Part" in "DownInterRep" packet.
 * @param p Pointer to the packet.
 * @param Part Value of "Part" to be set in packet.
 */
static inline void setLptDownInterRepPart(void* p, CrPsSixteenBit_t Part) {
   DownInterRep_t* t;
   t = (DownInterRep_t*)p;
   t->Part = __builtin_bswap16(Part);
}

/**
 * Get "Transid" from "DownLastRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "Transid".
 */
static inline CrPsTransId_t getLptDownLastRepTransid(void* p) {
   DownLastRep_t* t;
   t = (DownLastRep_t*)p;
   return __builtin_bswap16(t->Transid);
}

/**
 * Set "Transid" in "DownLastRep" packet.
 * @param p Pointer to the packet.
 * @param Transid Value of "Transid" to be set in packet.
 */
static inline void setLptDownLastRepTransid(void* p, CrPsTransId_t Transid) {
   DownLastRep_t* t;
   t = (DownLastRep_t*)p;
   t->Transid = __builtin_bswap16(Transid);
}

/**
 * Get "PartSeqNmb" from "DownLastRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "PartSeqNmb".
 */
static inline CrPsPartSeqNmb_t getLptDownLastRepPartSeqNmb(void* p) {
   DownLastRep_t* t;
   t = (DownLastRep_t*)p;
   return __builtin_bswap16(t->PartSeqNmb);
}

/**
 * Set "PartSeqNmb" in "DownLastRep" packet.
 * @param p Pointer to the packet.
 * @param PartSeqNmb Value of "PartSeqNmb" to be set in packet.
 */
static inline void setLptDownLastRepPartSeqNmb(void* p, CrPsPartSeqNmb_t PartSeqNmb) {
   DownLastRep_t* t;
   t = (DownLastRep_t*)p;
   t->PartSeqNmb = __builtin_bswap16(PartSeqNmb);
}

/**
 * Get "Part" from "DownLastRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "Part".
 */
static inline CrPsSixteenBit_t getLptDownLastRepPart(void* p) {
   DownLastRep_t* t;
   t = (DownLastRep_t*)p;
   return __builtin_bswap16(t->Part);
}

/**
 * Set "Part" in "DownLastRep" packet.
 * @param p Pointer to the packet.
 * @param Part Value of "Part" to be set in packet.
 */
static inline void setLptDownLastRepPart(void* p, CrPsSixteenBit_t Part) {
   DownLastRep_t* t;
   t = (DownLastRep_t*)p;
   t->Part = __builtin_bswap16(Part);
}

/**
 * Get "Transid" from "UpFirstCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "Transid".
 */
static inline CrPsTransId_t getLptUpFirstCmdTransid(void* p) {
   UpFirstCmd_t* t;
   t = (UpFirstCmd_t*)p;
   return __builtin_bswap16(t->Transid);
}

/**
 * Set "Transid" in "UpFirstCmd" packet.
 * @param p Pointer to the packet.
 * @param Transid Value of "Transid" to be set in packet.
 */
static inline void setLptUpFirstCmdTransid(void* p, CrPsTransId_t Transid) {
   UpFirstCmd_t* t;
   t = (UpFirstCmd_t*)p;
   t->Transid = __builtin_bswap16(Transid);
}

/**
 * Get "PartSeqNmb" from "UpFirstCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "PartSeqNmb".
 */
static inline CrPsPartSeqNmb_t getLptUpFirstCmdPartSeqNmb(void* p) {
   UpFirstCmd_t* t;
   t = (UpFirstCmd_t*)p;
   return __builtin_bswap16(t->PartSeqNmb);
}

/**
 * Set "PartSeqNmb" in "UpFirstCmd" packet.
 * @param p Pointer to the packet.
 * @param PartSeqNmb Value of "PartSeqNmb" to be set in packet.
 */
static inline void setLptUpFirstCmdPartSeqNmb(void* p, CrPsPartSeqNmb_t PartSeqNmb) {
   UpFirstCmd_t* t;
   t = (UpFirstCmd_t*)p;
   t->PartSeqNmb = __builtin_bswap16(PartSeqNmb);
}

/**
 * Get "Part" from "UpFirstCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "Part".
 */
static inline CrPsSixteenBit_t getLptUpFirstCmdPart(void* p) {
   UpFirstCmd_t* t;
   t = (UpFirstCmd_t*)p;
   return __builtin_bswap16(t->Part);
}

/**
 * Set "Part" in "UpFirstCmd" packet.
 * @param p Pointer to the packet.
 * @param Part Value of "Part" to be set in packet.
 */
static inline void setLptUpFirstCmdPart(void* p, CrPsSixteenBit_t Part) {
   UpFirstCmd_t* t;
   t = (UpFirstCmd_t*)p;
   t->Part = __builtin_bswap16(Part);
}

/**
 * Get "Transid" from "UpInterCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "Transid".
 */
static inline CrPsTransId_t getLptUpInterCmdTransid(void* p) {
   UpInterCmd_t* t;
   t = (UpInterCmd_t*)p;
   return __builtin_bswap16(t->Transid);
}

/**
 * Set "Transid" in "UpInterCmd" packet.
 * @param p Pointer to the packet.
 * @param Transid Value of "Transid" to be set in packet.
 */
static inline void setLptUpInterCmdTransid(void* p, CrPsTransId_t Transid) {
   UpInterCmd_t* t;
   t = (UpInterCmd_t*)p;
   t->Transid = __builtin_bswap16(Transid);
}

/**
 * Get "PartSeqNmb" from "UpInterCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "PartSeqNmb".
 */
static inline CrPsPartSeqNmb_t getLptUpInterCmdPartSeqNmb(void* p) {
   UpInterCmd_t* t;
   t = (UpInterCmd_t*)p;
   return __builtin_bswap16(t->PartSeqNmb);
}

/**
 * Set "PartSeqNmb" in "UpInterCmd" packet.
 * @param p Pointer to the packet.
 * @param PartSeqNmb Value of "PartSeqNmb" to be set in packet.
 */
static inline void setLptUpInterCmdPartSeqNmb(void* p, CrPsPartSeqNmb_t PartSeqNmb) {
   UpInterCmd_t* t;
   t = (UpInterCmd_t*)p;
   t->PartSeqNmb = __builtin_bswap16(PartSeqNmb);
}

/**
 * Get "Part" from "UpInterCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "Part".
 */
static inline CrPsSixteenBit_t getLptUpInterCmdPart(void* p) {
   UpInterCmd_t* t;
   t = (UpInterCmd_t*)p;
   return __builtin_bswap16(t->Part);
}

/**
 * Set "Part" in "UpInterCmd" packet.
 * @param p Pointer to the packet.
 * @param Part Value of "Part" to be set in packet.
 */
static inline void setLptUpInterCmdPart(void* p, CrPsSixteenBit_t Part) {
   UpInterCmd_t* t;
   t = (UpInterCmd_t*)p;
   t->Part = __builtin_bswap16(Part);
}

/**
 * Get "Transid" from "UpLastCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "Transid".
 */
static inline CrPsTransId_t getLptUpLastCmdTransid(void* p) {
   UpLastCmd_t* t;
   t = (UpLastCmd_t*)p;
   return __builtin_bswap16(t->Transid);
}

/**
 * Set "Transid" in "UpLastCmd" packet.
 * @param p Pointer to the packet.
 * @param Transid Value of "Transid" to be set in packet.
 */
static inline void setLptUpLastCmdTransid(void* p, CrPsTransId_t Transid) {
   UpLastCmd_t* t;
   t = (UpLastCmd_t*)p;
   t->Transid = __builtin_bswap16(Transid);
}

/**
 * Get "PartSeqNmb" from "UpLastCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "PartSeqNmb".
 */
static inline CrPsPartSeqNmb_t getLptUpLastCmdPartSeqNmb(void* p) {
   UpLastCmd_t* t;
   t = (UpLastCmd_t*)p;
   return __builtin_bswap16(t->PartSeqNmb);
}

/**
 * Set "PartSeqNmb" in "UpLastCmd" packet.
 * @param p Pointer to the packet.
 * @param PartSeqNmb Value of "PartSeqNmb" to be set in packet.
 */
static inline void setLptUpLastCmdPartSeqNmb(void* p, CrPsPartSeqNmb_t PartSeqNmb) {
   UpLastCmd_t* t;
   t = (UpLastCmd_t*)p;
   t->PartSeqNmb = __builtin_bswap16(PartSeqNmb);
}

/**
 * Get "Part" from "UpLastCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "Part".
 */
static inline CrPsSixteenBit_t getLptUpLastCmdPart(void* p) {
   UpLastCmd_t* t;
   t = (UpLastCmd_t*)p;
   return __builtin_bswap16(t->Part);
}

/**
 * Set "Part" in "UpLastCmd" packet.
 * @param p Pointer to the packet.
 * @param Part Value of "Part" to be set in packet.
 */
static inline void setLptUpLastCmdPart(void* p, CrPsSixteenBit_t Part) {
   UpLastCmd_t* t;
   t = (UpLastCmd_t*)p;
   t->Part = __builtin_bswap16(Part);
}

/**
 * Get "Transid" from "UpAbortRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "Transid".
 */
static inline CrPsTransId_t getLptUpAbortRepTransid(void* p) {
   UpAbortRep_t* t;
   t = (UpAbortRep_t*)p;
   return __builtin_bswap16(t->Transid);
}

/**
 * Set "Transid" in "UpAbortRep" packet.
 * @param p Pointer to the packet.
 * @param Transid Value of "Transid" to be set in packet.
 */
static inline void setLptUpAbortRepTransid(void* p, CrPsTransId_t Transid) {
   UpAbortRep_t* t;
   t = (UpAbortRep_t*)p;
   t->Transid = __builtin_bswap16(Transid);
}

/**
 * Get "FailReason" from "UpAbortRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "FailReason".
 */
static inline CrPsFailReason_t getLptUpAbortRepFailReason(void* p) {
   UpAbortRep_t* t;
   t = (UpAbortRep_t*)p;
   return __builtin_bswap16(t->FailReason);
}

/**
 * Set "FailReason" in "UpAbortRep" packet.
 * @param p Pointer to the packet.
 * @param FailReason Value of "FailReason" to be set in packet.
 */
static inline void setLptUpAbortRepFailReason(void* p, CrPsFailReason_t FailReason) {
   UpAbortRep_t* t;
   t = (UpAbortRep_t*)p;
   t->FailReason = __builtin_bswap16(FailReason);
}

/**
 * Get "Transid" from "StartDownCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "Transid".
 */
static inline CrPsTransId_t getLptStartDownCmdTransid(void* p) {
   StartDownCmd_t* t;
   t = (StartDownCmd_t*)p;
   return __builtin_bswap16(t->Transid);
}

/**
 * Set "Transid" in "StartDownCmd" packet.
 * @param p Pointer to the packet.
 * @param Transid Value of "Transid" to be set in packet.
 */
static inline void setLptStartDownCmdTransid(void* p, CrPsTransId_t Transid) {
   StartDownCmd_t* t;
   t = (StartDownCmd_t*)p;
   t->Transid = __builtin_bswap16(Transid);
}

/**
 * Get "Transid" from "AbortDownCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "Transid".
 */
static inline CrPsTransId_t getLptAbortDownCmdTransid(void* p) {
   AbortDownCmd_t* t;
   t = (AbortDownCmd_t*)p;
   return __builtin_bswap16(t->Transid);
}

/**
 * Set "Transid" in "AbortDownCmd" packet.
 * @param p Pointer to the packet.
 * @param Transid Value of "Transid" to be set in packet.
 */
static inline void setLptAbortDownCmdTransid(void* p, CrPsTransId_t Transid) {
   AbortDownCmd_t* t;
   t = (AbortDownCmd_t*)p;
   t->Transid = __builtin_bswap16(Transid);
}


/*----------------------------------------------------------------------------*/
#endif /* CRPSPCKTLPT_H */
