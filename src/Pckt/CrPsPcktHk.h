/**
 * @file
 * @ingroup gen_pck
 *
 * Interface for accessing fields in packets of service "Hk".
 *
 * @note This file was generated on 2018-10-28 17:05:47
 * @author Automatically Generated by CORDET Editor
 * @copyright P&P Software GmbH
 */
#ifndef CRPSPCKTHK_H_
#define CRPSPCKTHK_H_

#include "CrPsTypes.h"

#include "CrPsPckt.h"
/**
 * Structure for CreHkCmd
 */
typedef struct __attribute__((packed)) _CreHkCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * The structure identifier (SID) of the packet to be created
    */
   CrPsSID_t SID;
   
   /**
    * Collection Interval
    */
   CrPsCollectInterval_t CollectionInterval;
   
   /**
    * The number of parameters in the housekeeping report to be created
    */
   CrPsNPar_t N1;
   
   struct __attribute__((packed)) {
      /**
       * The identifiers of the simply commutated parameters in the report to
       * be created
       */
      CrPsParId_t N1ParamId;
      
   } N1_[0];
   /**
    * The number of super-commutated groups of parameters
    */
   CrPsNGroups_t NFA;
   
   struct __attribute__((packed)) {
      /**
       * Super Commutated Sample Repetition Number (repeated NFA times)
       */
      CrPsRepNum_t SCSampleRepNum;
      
      /**
       * The number of parameters in the super-commutated group
       */
      CrPsNPar_t N2;
      
      struct __attribute__((packed)) {
         /**
          * Parameter ID
          */
         CrPsParId_t N2ParamId;
         
      } N2_[0];
   } NFA_[0];
} CreHkCmd_t ;

/**
 * Structure for CreDiagCmd
 */
typedef struct __attribute__((packed)) _CreDiagCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * The structure identifier (SID) of the packet to be created
    */
   CrPsSID_t SID;
   
   /**
    * Collection Interval
    */
   CrPsCollectInterval_t CollectionInterval;
   
   /**
    * The number of parameters in the diagnostic report to be created
    */
   CrPsNPar_t N1;
   
   struct __attribute__((packed)) {
      /**
       * The identifiers of the simply commutated parameters in the report to
       * be created
       */
      CrPsParId_t N1ParamId;
      
   } N1_[0];
   /**
    * The number of super-commutated groups of parameters
    */
   CrPsNGroups_t NFA;
   
   struct __attribute__((packed)) {
      /**
       * Super Commutated Sample Repetition Number (repeated NFA times)
       */
      CrPsRepNum_t SCSampleRepNum;
      
      /**
       * The number of parameters in the super-commutated group
       */
      CrPsNPar_t N2;
      
      struct __attribute__((packed)) {
         /**
          * Parameter ID
          */
         CrPsParId_t N2ParamId;
         
      } N2_[0];
   } NFA_[0];
} CreDiagCmd_t ;

/**
 * Structure for DelHkCmd
 */
typedef struct __attribute__((packed)) _DelHkCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * The number of report definitions to be deleted
    */
   CrPsNSID_t N;
   
   struct __attribute__((packed)) {
      /**
       * The structure identifiers (SIDs) of the report definitions to be
       * deleted
       */
      CrPsSID_t SID;
      
   } N_[0];
} DelHkCmd_t ;

/**
 * Structure for DelDiagCmd
 */
typedef struct __attribute__((packed)) _DelDiagCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * The number of report definitions to be deleted
    */
   CrPsNSID_t N;
   
   struct __attribute__((packed)) {
      /**
       * The structure identifier (SID) of a report to be deleted
       */
      CrPsSID_t SID;
      
   } N_[0];
} DelDiagCmd_t ;

/**
 * Structure for EnbHkCmd
 */
typedef struct __attribute__((packed)) _EnbHkCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Number of SIDs to be enabled
    */
   CrPsNSID_t N;
   
   /**
    * SID to be enabled
    */
   CrPsSID_t SID;
   
} EnbHkCmd_t ;

/**
 * Structure for DisHkCmd
 */
typedef struct __attribute__((packed)) _DisHkCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Number of SIDs to be disabled
    */
   CrPsNSID_t N;
   
   struct __attribute__((packed)) {
      /**
       * SID to be disabled
       */
      CrPsSID_t SID;
      
   } N_[0];
} DisHkCmd_t ;

/**
 * Structure for EnbDiagCmd
 */
typedef struct __attribute__((packed)) _EnbDiagCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Number of SIDs to be enabled
    */
   CrPsNSID_t N;
   
   /**
    * SID to be enabled
    */
   CrPsSID_t SID;
   
} EnbDiagCmd_t ;

/**
 * Structure for DisDiagCmd
 */
typedef struct __attribute__((packed)) _DisDiagCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Number of SIDs to be disabled
    */
   CrPsNSID_t N;
   
   struct __attribute__((packed)) {
      /**
       * SID to be disabled
       */
      CrPsSID_t SID;
      
   } N_[0];
} DisDiagCmd_t ;

/**
 * Structure for RepStructHkCmd
 */
typedef struct __attribute__((packed)) _RepStructHkCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Number of SIDs to be reported
    */
   CrPsNSID_t N;
   
   struct __attribute__((packed)) {
      /**
       * SID to be reported
       */
      CrPsSID_t SID;
      
   } N_[0];
} RepStructHkCmd_t ;

/**
 * Structure for RepStructHkRep
 */
typedef struct __attribute__((packed)) _RepStructHkRep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Structure Identifier
    */
   CrPsSID_t SID;
   
   /**
    * Flag indicating whether periodic generation of the packet is enabled or
    * disabled
    */
   CrPsEnDis_t PerGenActionStatus;
   
   /**
    * Collection Interval
    */
   CrPsCollectInterval_t CollectionInterval;
   
   /**
    * The number of simply commutated parameters
    */
   CrPsNPar_t N1;
   
   struct __attribute__((packed)) {
      /**
       * Identifier of a simply commutated parameter
       */
      CrPsParId_t N1ParamId;
      
   } N1_[0];
   /**
    * The number of super-commutated groups of parameters
    */
   CrPsNGroups_t NFA;
   
   struct __attribute__((packed)) {
      /**
       * Super Commutated Sample Repetition Number (repeated NFA times)
       */
      CrPsRepNum_t SCSampleRepNum;
      
      /**
       * The number of parameters in the super-commutated group
       */
      CrPsNPar_t N2;
      
      struct __attribute__((packed)) {
         /**
          * Parameter ID
          */
         CrPsParId_t N2ParamId;
         
      } N2_[0];
   } NFA_[0];
} RepStructHkRep_t ;

/**
 * Structure for RepStructDiagCmd
 */
typedef struct __attribute__((packed)) _RepStructDiagCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Number of SIDs to be reported
    */
   CrPsNSID_t N;
   
   struct __attribute__((packed)) {
      /**
       * SID to be reported
       */
      CrPsSID_t SID;
      
   } N_[0];
} RepStructDiagCmd_t ;

/**
 * Structure for RepStructDiagRep
 */
typedef struct __attribute__((packed)) _RepStructDiagRep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Structure Identifier
    */
   CrPsSID_t SID;
   
   /**
    * Flag indicating whether periodic generation of the packet is enabled or
    * disabled
    */
   CrPsEnDis_t PerGenActionStatus;
   
   /**
    * Collection Interval
    */
   CrPsCollectInterval_t CollectionInterval;
   
   /**
    * The number of simply commutated parameters
    */
   CrPsNPar_t N1;
   
   struct __attribute__((packed)) {
      /**
       * Identifier of a simply commutated parameter
       */
      CrPsParId_t N1ParamId;
      
   } N1_[0];
   /**
    * The number of super-commutated groups of parameters
    */
   CrPsNGroups_t NFA;
   
   struct __attribute__((packed)) {
      /**
       * Super Commutated Sample Repetition Number (repeated NFA times)
       */
      CrPsRepNum_t SCSampleRepNum;
      
      /**
       * The number of parameters in the super-commutated group
       */
      CrPsNPar_t N2;
      
      struct __attribute__((packed)) {
         /**
          * Parameter ID
          */
         CrPsParId_t N2ParamId;
         
      } N2_[0];
   } NFA_[0];
} RepStructDiagRep_t ;

/**
 * Structure for Rep
 */
typedef struct __attribute__((packed)) _Rep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Structure Identifier
    */
   CrPsSID_t SID;
   
} Rep_t ;

/**
 * Structure for DiagRep
 */
typedef struct __attribute__((packed)) _DiagRep_t {
   /**
    * Packet header.
    */
   TmHeader_t Header;

   /**
    * Structure Identifier
    */
   CrPsSID_t SID;
   
} DiagRep_t ;

/**
 * Structure for OneShotHkCmd
 */
typedef struct __attribute__((packed)) _OneShotHkCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Number of SIDs to be generated in one-shot mode
    */
   CrPsNSID_t N;
   
   struct __attribute__((packed)) {
      /**
       * SID to be generated in one-shot mode
       */
      CrPsSID_t SID;
      
   } N_[0];
} OneShotHkCmd_t ;

/**
 * Structure for OneShotDiagCmd
 */
typedef struct __attribute__((packed)) _OneShotDiagCmd_t {
   /**
    * Packet header.
    */
   TcHeader_t Header;

   /**
    * Number of SIDs to be generated in one-shot mode
    */
   CrPsNSID_t N;
   
   struct __attribute__((packed)) {
      /**
       * SID to be generated in one-shot mode
       */
      CrPsSID_t SID;
      
   } N_[0];
} OneShotDiagCmd_t ;

/**
 * Get "SID" from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkCreHkCmdSID(void* p) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   return __builtin_bswap16(t->SID);
}

/**
 * Set "SID" in "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkCreHkCmdSID(void* p, CrPsSID_t SID) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   t->SID = __builtin_bswap16(SID);
}

/**
 * Get "CollectionInterval" from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "CollectionInterval".
 */
static inline CrPsCollectInterval_t getHkCreHkCmdCollectionInterval(void* p) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   return __builtin_bswap16(t->CollectionInterval);
}

/**
 * Set "CollectionInterval" in "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param CollectionInterval Value of "CollectionInterval" to be set in
 * packet.
 */
static inline void setHkCreHkCmdCollectionInterval(void* p, CrPsCollectInterval_t CollectionInterval) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   t->CollectionInterval = __builtin_bswap16(CollectionInterval);
}

/**
 * Get "N1" from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N1".
 */
static inline CrPsNPar_t getHkCreHkCmdN1(void* p) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   return t->N1;
}

/**
 * Set "N1" in "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N1 Value of "N1" to be set in packet.
 */
static inline void setHkCreHkCmdN1(void* p, CrPsNPar_t N1) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   t->N1 = N1;
}

/**
 * Get pointer to "N1ParamId" array from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the N1ParamId array.
 */
static inline CrPsParId_t* getHkCreHkCmdN1ParamIdArray(void* p) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   return &t->N1_[0].N1ParamId;
}

/**
 * Get "N1ParamId" array from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkCreHkCmdN1ParamIdArray(void* p, void* dest) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   memcpy(dest, &t->N1_[0].N1ParamId, t->N1*sizeof(t->N1_[0].N1ParamId));
}

/**
 * Set "N1ParamId" array in "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkCreHkCmdN1ParamIdArray(void* p, const void* src) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   memcpy(&t->N1_[0].N1ParamId, src, t->N1*sizeof(t->N1_[0].N1ParamId));
}

/**
 * Get "N1ParamId" from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N1
 * @return Value of "N1ParamId".
 */
static inline CrPsParId_t getHkCreHkCmdN1ParamId(void* p, CrPsNPar_t N1) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   return __builtin_bswap16(t->N1_[N1].N1ParamId);
}

/**
 * Set "N1ParamId" in "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N1
 * @param N1ParamId Value of "N1ParamId" to be set in packet.
 */
static inline void setHkCreHkCmdN1ParamId(void* p, CrPsNPar_t N1, CrPsParId_t N1ParamId) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   t->N1_[N1].N1ParamId = __builtin_bswap16(N1ParamId);
}

/**
 * Get "NFA" from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "NFA".
 */
static inline CrPsNGroups_t getHkCreHkCmdNFA(void* p) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   return t->NFA;
}

/**
 * Set "NFA" in "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA Value of "NFA" to be set in packet.
 */
static inline void setHkCreHkCmdNFA(void* p, CrPsNGroups_t NFA) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   t->NFA = NFA;
}

/**
 * Get "SCSampleRepNum" from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @return Value of "SCSampleRepNum".
 */
static inline CrPsRepNum_t getHkCreHkCmdSCSampleRepNum(void* p, CrPsNGroups_t NFA) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   return t->NFA_[NFA].SCSampleRepNum;
}

/**
 * Set "SCSampleRepNum" in "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param SCSampleRepNum Value of "SCSampleRepNum" to be set in packet.
 */
static inline void setHkCreHkCmdSCSampleRepNum(void* p, CrPsNGroups_t NFA, CrPsRepNum_t SCSampleRepNum) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   t->NFA_[NFA].SCSampleRepNum = SCSampleRepNum;
}

/**
 * Get "N2" from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N2".
 */
static inline CrPsNPar_t getHkCreHkCmdN2(void* p) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   return t->NFA_[NFA].N2;
}

/**
 * Set "N2" in "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N2 Value of "N2" to be set in packet.
 */
static inline void setHkCreHkCmdN2(void* p, CrPsNPar_t N2) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   t->NFA_[NFA].N2 = N2;
}

/**
 * Get pointer to "N2ParamId" array from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @return Pointer to the start of the N2ParamId array.
 */
static inline CrPsParId_t* getHkCreHkCmdN2ParamIdArray(void* p, CrPsNGroups_t NFA) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   return &t->NFA_[NFA].N2_[0].N2ParamId;
}

/**
 * Get "N2ParamId" array from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkCreHkCmdN2ParamIdArray(void* p, CrPsNGroups_t NFA, void* dest) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   memcpy(dest, &t->NFA_[NFA].N2_[0].N2ParamId, t->NFA_[NFA].N2*sizeof(t->NFA_[NFA].N2_[0].N2ParamId));
}

/**
 * Set "N2ParamId" array in "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkCreHkCmdN2ParamIdArray(void* p, CrPsNGroups_t NFA, const void* src) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   memcpy(&t->NFA_[NFA].N2_[0].N2ParamId, src, t->NFA_[NFA].N2*sizeof(t->NFA_[NFA].N2_[0].N2ParamId));
}

/**
 * Get "N2ParamId" from "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param N2
 * @return Value of "N2ParamId".
 */
static inline CrPsParId_t getHkCreHkCmdN2ParamId(void* p, CrPsNGroups_t NFA, CrPsNPar_t N2) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   return __builtin_bswap16(t->NFA_[NFA].N2_[N2].N2ParamId);
}

/**
 * Set "N2ParamId" in "CreHkCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param N2
 * @param N2ParamId Value of "N2ParamId" to be set in packet.
 */
static inline void setHkCreHkCmdN2ParamId(void* p, CrPsNGroups_t NFA, CrPsNPar_t N2, CrPsParId_t N2ParamId) {
   CreHkCmd_t* t;
   t = (CreHkCmd_t*)p;
   t->NFA_[NFA].N2_[N2].N2ParamId = __builtin_bswap16(N2ParamId);
}

/**
 * Get "SID" from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkCreDiagCmdSID(void* p) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   return __builtin_bswap16(t->SID);
}

/**
 * Set "SID" in "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkCreDiagCmdSID(void* p, CrPsSID_t SID) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   t->SID = __builtin_bswap16(SID);
}

/**
 * Get "CollectionInterval" from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "CollectionInterval".
 */
static inline CrPsCollectInterval_t getHkCreDiagCmdCollectionInterval(void* p) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   return __builtin_bswap16(t->CollectionInterval);
}

/**
 * Set "CollectionInterval" in "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param CollectionInterval Value of "CollectionInterval" to be set in
 * packet.
 */
static inline void setHkCreDiagCmdCollectionInterval(void* p, CrPsCollectInterval_t CollectionInterval) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   t->CollectionInterval = __builtin_bswap16(CollectionInterval);
}

/**
 * Get "N1" from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N1".
 */
static inline CrPsNPar_t getHkCreDiagCmdN1(void* p) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   return t->N1;
}

/**
 * Set "N1" in "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N1 Value of "N1" to be set in packet.
 */
static inline void setHkCreDiagCmdN1(void* p, CrPsNPar_t N1) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   t->N1 = N1;
}

/**
 * Get pointer to "N1ParamId" array from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the N1ParamId array.
 */
static inline CrPsParId_t* getHkCreDiagCmdN1ParamIdArray(void* p) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   return &t->N1_[0].N1ParamId;
}

/**
 * Get "N1ParamId" array from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkCreDiagCmdN1ParamIdArray(void* p, void* dest) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   memcpy(dest, &t->N1_[0].N1ParamId, t->N1*sizeof(t->N1_[0].N1ParamId));
}

/**
 * Set "N1ParamId" array in "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkCreDiagCmdN1ParamIdArray(void* p, const void* src) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   memcpy(&t->N1_[0].N1ParamId, src, t->N1*sizeof(t->N1_[0].N1ParamId));
}

/**
 * Get "N1ParamId" from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N1
 * @return Value of "N1ParamId".
 */
static inline CrPsParId_t getHkCreDiagCmdN1ParamId(void* p, CrPsNPar_t N1) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   return __builtin_bswap16(t->N1_[N1].N1ParamId);
}

/**
 * Set "N1ParamId" in "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N1
 * @param N1ParamId Value of "N1ParamId" to be set in packet.
 */
static inline void setHkCreDiagCmdN1ParamId(void* p, CrPsNPar_t N1, CrPsParId_t N1ParamId) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   t->N1_[N1].N1ParamId = __builtin_bswap16(N1ParamId);
}

/**
 * Get "NFA" from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "NFA".
 */
static inline CrPsNGroups_t getHkCreDiagCmdNFA(void* p) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   return t->NFA;
}

/**
 * Set "NFA" in "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA Value of "NFA" to be set in packet.
 */
static inline void setHkCreDiagCmdNFA(void* p, CrPsNGroups_t NFA) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   t->NFA = NFA;
}

/**
 * Get "SCSampleRepNum" from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @return Value of "SCSampleRepNum".
 */
static inline CrPsRepNum_t getHkCreDiagCmdSCSampleRepNum(void* p, CrPsNGroups_t NFA) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   return t->NFA_[NFA].SCSampleRepNum;
}

/**
 * Set "SCSampleRepNum" in "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param SCSampleRepNum Value of "SCSampleRepNum" to be set in packet.
 */
static inline void setHkCreDiagCmdSCSampleRepNum(void* p, CrPsNGroups_t NFA, CrPsRepNum_t SCSampleRepNum) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   t->NFA_[NFA].SCSampleRepNum = SCSampleRepNum;
}

/**
 * Get "N2" from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N2".
 */
static inline CrPsNPar_t getHkCreDiagCmdN2(void* p) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   return t->NFA_[NFA].N2;
}

/**
 * Set "N2" in "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N2 Value of "N2" to be set in packet.
 */
static inline void setHkCreDiagCmdN2(void* p, CrPsNPar_t N2) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   t->NFA_[NFA].N2 = N2;
}

/**
 * Get pointer to "N2ParamId" array from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @return Pointer to the start of the N2ParamId array.
 */
static inline CrPsParId_t* getHkCreDiagCmdN2ParamIdArray(void* p, CrPsNGroups_t NFA) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   return &t->NFA_[NFA].N2_[0].N2ParamId;
}

/**
 * Get "N2ParamId" array from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkCreDiagCmdN2ParamIdArray(void* p, CrPsNGroups_t NFA, void* dest) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   memcpy(dest, &t->NFA_[NFA].N2_[0].N2ParamId, t->NFA_[NFA].N2*sizeof(t->NFA_[NFA].N2_[0].N2ParamId));
}

/**
 * Set "N2ParamId" array in "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkCreDiagCmdN2ParamIdArray(void* p, CrPsNGroups_t NFA, const void* src) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   memcpy(&t->NFA_[NFA].N2_[0].N2ParamId, src, t->NFA_[NFA].N2*sizeof(t->NFA_[NFA].N2_[0].N2ParamId));
}

/**
 * Get "N2ParamId" from "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param N2
 * @return Value of "N2ParamId".
 */
static inline CrPsParId_t getHkCreDiagCmdN2ParamId(void* p, CrPsNGroups_t NFA, CrPsNPar_t N2) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   return __builtin_bswap16(t->NFA_[NFA].N2_[N2].N2ParamId);
}

/**
 * Set "N2ParamId" in "CreDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param N2
 * @param N2ParamId Value of "N2ParamId" to be set in packet.
 */
static inline void setHkCreDiagCmdN2ParamId(void* p, CrPsNGroups_t NFA, CrPsNPar_t N2, CrPsParId_t N2ParamId) {
   CreDiagCmd_t* t;
   t = (CreDiagCmd_t*)p;
   t->NFA_[NFA].N2_[N2].N2ParamId = __builtin_bswap16(N2ParamId);
}

/**
 * Get "N" from "DelHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNSID_t getHkDelHkCmdN(void* p) {
   DelHkCmd_t* t;
   t = (DelHkCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "DelHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setHkDelHkCmdN(void* p, CrPsNSID_t N) {
   DelHkCmd_t* t;
   t = (DelHkCmd_t*)p;
   t->N = N;
}

/**
 * Get pointer to "SID" array from "DelHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the SID array.
 */
static inline CrPsSID_t* getHkDelHkCmdSIDArray(void* p) {
   DelHkCmd_t* t;
   t = (DelHkCmd_t*)p;
   return &t->N_[0].SID;
}

/**
 * Get "SID" array from "DelHkCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkDelHkCmdSIDArray(void* p, void* dest) {
   DelHkCmd_t* t;
   t = (DelHkCmd_t*)p;
   memcpy(dest, &t->N_[0].SID, t->N*sizeof(t->N_[0].SID));
}

/**
 * Set "SID" array in "DelHkCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkDelHkCmdSIDArray(void* p, const void* src) {
   DelHkCmd_t* t;
   t = (DelHkCmd_t*)p;
   memcpy(&t->N_[0].SID, src, t->N*sizeof(t->N_[0].SID));
}

/**
 * Get "SID" from "DelHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkDelHkCmdSID(void* p, CrPsNSID_t N) {
   DelHkCmd_t* t;
   t = (DelHkCmd_t*)p;
   return __builtin_bswap16(t->N_[N].SID);
}

/**
 * Set "SID" in "DelHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkDelHkCmdSID(void* p, CrPsNSID_t N, CrPsSID_t SID) {
   DelHkCmd_t* t;
   t = (DelHkCmd_t*)p;
   t->N_[N].SID = __builtin_bswap16(SID);
}

/**
 * Get "N" from "DelDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNSID_t getHkDelDiagCmdN(void* p) {
   DelDiagCmd_t* t;
   t = (DelDiagCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "DelDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setHkDelDiagCmdN(void* p, CrPsNSID_t N) {
   DelDiagCmd_t* t;
   t = (DelDiagCmd_t*)p;
   t->N = N;
}

/**
 * Get pointer to "SID" array from "DelDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the SID array.
 */
static inline CrPsSID_t* getHkDelDiagCmdSIDArray(void* p) {
   DelDiagCmd_t* t;
   t = (DelDiagCmd_t*)p;
   return &t->N_[0].SID;
}

/**
 * Get "SID" array from "DelDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkDelDiagCmdSIDArray(void* p, void* dest) {
   DelDiagCmd_t* t;
   t = (DelDiagCmd_t*)p;
   memcpy(dest, &t->N_[0].SID, t->N*sizeof(t->N_[0].SID));
}

/**
 * Set "SID" array in "DelDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkDelDiagCmdSIDArray(void* p, const void* src) {
   DelDiagCmd_t* t;
   t = (DelDiagCmd_t*)p;
   memcpy(&t->N_[0].SID, src, t->N*sizeof(t->N_[0].SID));
}

/**
 * Get "SID" from "DelDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkDelDiagCmdSID(void* p, CrPsNSID_t N) {
   DelDiagCmd_t* t;
   t = (DelDiagCmd_t*)p;
   return __builtin_bswap16(t->N_[N].SID);
}

/**
 * Set "SID" in "DelDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkDelDiagCmdSID(void* p, CrPsNSID_t N, CrPsSID_t SID) {
   DelDiagCmd_t* t;
   t = (DelDiagCmd_t*)p;
   t->N_[N].SID = __builtin_bswap16(SID);
}

/**
 * Get "N" from "EnbHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNSID_t getHkEnbHkCmdN(void* p) {
   EnbHkCmd_t* t;
   t = (EnbHkCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "EnbHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setHkEnbHkCmdN(void* p, CrPsNSID_t N) {
   EnbHkCmd_t* t;
   t = (EnbHkCmd_t*)p;
   t->N = N;
}

/**
 * Get "SID" from "EnbHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkEnbHkCmdSID(void* p) {
   EnbHkCmd_t* t;
   t = (EnbHkCmd_t*)p;
   return __builtin_bswap16(t->SID);
}

/**
 * Set "SID" in "EnbHkCmd" packet.
 * @param p Pointer to the packet.
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkEnbHkCmdSID(void* p, CrPsSID_t SID) {
   EnbHkCmd_t* t;
   t = (EnbHkCmd_t*)p;
   t->SID = __builtin_bswap16(SID);
}

/**
 * Get "N" from "DisHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNSID_t getHkDisHkCmdN(void* p) {
   DisHkCmd_t* t;
   t = (DisHkCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "DisHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setHkDisHkCmdN(void* p, CrPsNSID_t N) {
   DisHkCmd_t* t;
   t = (DisHkCmd_t*)p;
   t->N = N;
}

/**
 * Get pointer to "SID" array from "DisHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the SID array.
 */
static inline CrPsSID_t* getHkDisHkCmdSIDArray(void* p) {
   DisHkCmd_t* t;
   t = (DisHkCmd_t*)p;
   return &t->N_[0].SID;
}

/**
 * Get "SID" array from "DisHkCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkDisHkCmdSIDArray(void* p, void* dest) {
   DisHkCmd_t* t;
   t = (DisHkCmd_t*)p;
   memcpy(dest, &t->N_[0].SID, t->N*sizeof(t->N_[0].SID));
}

/**
 * Set "SID" array in "DisHkCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkDisHkCmdSIDArray(void* p, const void* src) {
   DisHkCmd_t* t;
   t = (DisHkCmd_t*)p;
   memcpy(&t->N_[0].SID, src, t->N*sizeof(t->N_[0].SID));
}

/**
 * Get "SID" from "DisHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkDisHkCmdSID(void* p, CrPsNSID_t N) {
   DisHkCmd_t* t;
   t = (DisHkCmd_t*)p;
   return __builtin_bswap16(t->N_[N].SID);
}

/**
 * Set "SID" in "DisHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkDisHkCmdSID(void* p, CrPsNSID_t N, CrPsSID_t SID) {
   DisHkCmd_t* t;
   t = (DisHkCmd_t*)p;
   t->N_[N].SID = __builtin_bswap16(SID);
}

/**
 * Get "N" from "EnbDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNSID_t getHkEnbDiagCmdN(void* p) {
   EnbDiagCmd_t* t;
   t = (EnbDiagCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "EnbDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setHkEnbDiagCmdN(void* p, CrPsNSID_t N) {
   EnbDiagCmd_t* t;
   t = (EnbDiagCmd_t*)p;
   t->N = N;
}

/**
 * Get "SID" from "EnbDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkEnbDiagCmdSID(void* p) {
   EnbDiagCmd_t* t;
   t = (EnbDiagCmd_t*)p;
   return __builtin_bswap16(t->SID);
}

/**
 * Set "SID" in "EnbDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkEnbDiagCmdSID(void* p, CrPsSID_t SID) {
   EnbDiagCmd_t* t;
   t = (EnbDiagCmd_t*)p;
   t->SID = __builtin_bswap16(SID);
}

/**
 * Get "N" from "DisDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNSID_t getHkDisDiagCmdN(void* p) {
   DisDiagCmd_t* t;
   t = (DisDiagCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "DisDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setHkDisDiagCmdN(void* p, CrPsNSID_t N) {
   DisDiagCmd_t* t;
   t = (DisDiagCmd_t*)p;
   t->N = N;
}

/**
 * Get pointer to "SID" array from "DisDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the SID array.
 */
static inline CrPsSID_t* getHkDisDiagCmdSIDArray(void* p) {
   DisDiagCmd_t* t;
   t = (DisDiagCmd_t*)p;
   return &t->N_[0].SID;
}

/**
 * Get "SID" array from "DisDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkDisDiagCmdSIDArray(void* p, void* dest) {
   DisDiagCmd_t* t;
   t = (DisDiagCmd_t*)p;
   memcpy(dest, &t->N_[0].SID, t->N*sizeof(t->N_[0].SID));
}

/**
 * Set "SID" array in "DisDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkDisDiagCmdSIDArray(void* p, const void* src) {
   DisDiagCmd_t* t;
   t = (DisDiagCmd_t*)p;
   memcpy(&t->N_[0].SID, src, t->N*sizeof(t->N_[0].SID));
}

/**
 * Get "SID" from "DisDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkDisDiagCmdSID(void* p, CrPsNSID_t N) {
   DisDiagCmd_t* t;
   t = (DisDiagCmd_t*)p;
   return __builtin_bswap16(t->N_[N].SID);
}

/**
 * Set "SID" in "DisDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkDisDiagCmdSID(void* p, CrPsNSID_t N, CrPsSID_t SID) {
   DisDiagCmd_t* t;
   t = (DisDiagCmd_t*)p;
   t->N_[N].SID = __builtin_bswap16(SID);
}

/**
 * Get "N" from "RepStructHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNSID_t getHkRepStructHkCmdN(void* p) {
   RepStructHkCmd_t* t;
   t = (RepStructHkCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "RepStructHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setHkRepStructHkCmdN(void* p, CrPsNSID_t N) {
   RepStructHkCmd_t* t;
   t = (RepStructHkCmd_t*)p;
   t->N = N;
}

/**
 * Get pointer to "SID" array from "RepStructHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the SID array.
 */
static inline CrPsSID_t* getHkRepStructHkCmdSIDArray(void* p) {
   RepStructHkCmd_t* t;
   t = (RepStructHkCmd_t*)p;
   return &t->N_[0].SID;
}

/**
 * Get "SID" array from "RepStructHkCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkRepStructHkCmdSIDArray(void* p, void* dest) {
   RepStructHkCmd_t* t;
   t = (RepStructHkCmd_t*)p;
   memcpy(dest, &t->N_[0].SID, t->N*sizeof(t->N_[0].SID));
}

/**
 * Set "SID" array in "RepStructHkCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkRepStructHkCmdSIDArray(void* p, const void* src) {
   RepStructHkCmd_t* t;
   t = (RepStructHkCmd_t*)p;
   memcpy(&t->N_[0].SID, src, t->N*sizeof(t->N_[0].SID));
}

/**
 * Get "SID" from "RepStructHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkRepStructHkCmdSID(void* p, CrPsNSID_t N) {
   RepStructHkCmd_t* t;
   t = (RepStructHkCmd_t*)p;
   return __builtin_bswap16(t->N_[N].SID);
}

/**
 * Set "SID" in "RepStructHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkRepStructHkCmdSID(void* p, CrPsNSID_t N, CrPsSID_t SID) {
   RepStructHkCmd_t* t;
   t = (RepStructHkCmd_t*)p;
   t->N_[N].SID = __builtin_bswap16(SID);
}

/**
 * Get "SID" from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkRepStructHkRepSID(void* p) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return __builtin_bswap16(t->SID);
}

/**
 * Set "SID" in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkRepStructHkRepSID(void* p, CrPsSID_t SID) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   t->SID = __builtin_bswap16(SID);
}

/**
 * Get "PerGenActionStatus" from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "PerGenActionStatus".
 */
static inline CrPsEnDis_t getHkRepStructHkRepPerGenActionStatus(void* p) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return t->PerGenActionStatus;
}

/**
 * Set "PerGenActionStatus" in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param PerGenActionStatus Value of "PerGenActionStatus" to be set in
 * packet.
 */
static inline void setHkRepStructHkRepPerGenActionStatus(void* p, CrPsEnDis_t PerGenActionStatus) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   t->PerGenActionStatus = PerGenActionStatus;
}

/**
 * Get "CollectionInterval" from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "CollectionInterval".
 */
static inline CrPsCollectInterval_t getHkRepStructHkRepCollectionInterval(void* p) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return __builtin_bswap16(t->CollectionInterval);
}

/**
 * Set "CollectionInterval" in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param CollectionInterval Value of "CollectionInterval" to be set in
 * packet.
 */
static inline void setHkRepStructHkRepCollectionInterval(void* p, CrPsCollectInterval_t CollectionInterval) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   t->CollectionInterval = __builtin_bswap16(CollectionInterval);
}

/**
 * Get "N1" from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "N1".
 */
static inline CrPsNPar_t getHkRepStructHkRepN1(void* p) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return t->N1;
}

/**
 * Set "N1" in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param N1 Value of "N1" to be set in packet.
 */
static inline void setHkRepStructHkRepN1(void* p, CrPsNPar_t N1) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   t->N1 = N1;
}

/**
 * Get pointer to "N1ParamId" array from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the N1ParamId array.
 */
static inline CrPsParId_t* getHkRepStructHkRepN1ParamIdArray(void* p) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return &t->N1_[0].N1ParamId;
}

/**
 * Get "N1ParamId" array from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkRepStructHkRepN1ParamIdArray(void* p, void* dest) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   memcpy(dest, &t->N1_[0].N1ParamId, t->N1*sizeof(t->N1_[0].N1ParamId));
}

/**
 * Set "N1ParamId" array in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkRepStructHkRepN1ParamIdArray(void* p, const void* src) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   memcpy(&t->N1_[0].N1ParamId, src, t->N1*sizeof(t->N1_[0].N1ParamId));
}

/**
 * Get "N1ParamId" from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param N1
 * @return Value of "N1ParamId".
 */
static inline CrPsParId_t getHkRepStructHkRepN1ParamId(void* p, CrPsNPar_t N1) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return __builtin_bswap16(t->N1_[N1].N1ParamId);
}

/**
 * Set "N1ParamId" in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param N1
 * @param N1ParamId Value of "N1ParamId" to be set in packet.
 */
static inline void setHkRepStructHkRepN1ParamId(void* p, CrPsNPar_t N1, CrPsParId_t N1ParamId) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   t->N1_[N1].N1ParamId = __builtin_bswap16(N1ParamId);
}

/**
 * Get "NFA" from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "NFA".
 */
static inline CrPsNGroups_t getHkRepStructHkRepNFA(void* p) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return t->NFA;
}

/**
 * Set "NFA" in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param NFA Value of "NFA" to be set in packet.
 */
static inline void setHkRepStructHkRepNFA(void* p, CrPsNGroups_t NFA) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   t->NFA = NFA;
}

/**
 * Get "SCSampleRepNum" from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @return Value of "SCSampleRepNum".
 */
static inline CrPsRepNum_t getHkRepStructHkRepSCSampleRepNum(void* p, CrPsNGroups_t NFA) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return t->NFA_[NFA].SCSampleRepNum;
}

/**
 * Set "SCSampleRepNum" in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param SCSampleRepNum Value of "SCSampleRepNum" to be set in packet.
 */
static inline void setHkRepStructHkRepSCSampleRepNum(void* p, CrPsNGroups_t NFA, CrPsRepNum_t SCSampleRepNum) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   t->NFA_[NFA].SCSampleRepNum = SCSampleRepNum;
}

/**
 * Get "N2" from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "N2".
 */
static inline CrPsNPar_t getHkRepStructHkRepN2(void* p) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return t->NFA_[NFA].N2;
}

/**
 * Set "N2" in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param N2 Value of "N2" to be set in packet.
 */
static inline void setHkRepStructHkRepN2(void* p, CrPsNPar_t N2) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   t->NFA_[NFA].N2 = N2;
}

/**
 * Get pointer to "N2ParamId" array from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @return Pointer to the start of the N2ParamId array.
 */
static inline CrPsParId_t* getHkRepStructHkRepN2ParamIdArray(void* p, CrPsNGroups_t NFA) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return &t->NFA_[NFA].N2_[0].N2ParamId;
}

/**
 * Get "N2ParamId" array from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkRepStructHkRepN2ParamIdArray(void* p, CrPsNGroups_t NFA, void* dest) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   memcpy(dest, &t->NFA_[NFA].N2_[0].N2ParamId, t->NFA_[NFA].N2*sizeof(t->NFA_[NFA].N2_[0].N2ParamId));
}

/**
 * Set "N2ParamId" array in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkRepStructHkRepN2ParamIdArray(void* p, CrPsNGroups_t NFA, const void* src) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   memcpy(&t->NFA_[NFA].N2_[0].N2ParamId, src, t->NFA_[NFA].N2*sizeof(t->NFA_[NFA].N2_[0].N2ParamId));
}

/**
 * Get "N2ParamId" from "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param N2
 * @return Value of "N2ParamId".
 */
static inline CrPsParId_t getHkRepStructHkRepN2ParamId(void* p, CrPsNGroups_t NFA, CrPsNPar_t N2) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   return __builtin_bswap16(t->NFA_[NFA].N2_[N2].N2ParamId);
}

/**
 * Set "N2ParamId" in "RepStructHkRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param N2
 * @param N2ParamId Value of "N2ParamId" to be set in packet.
 */
static inline void setHkRepStructHkRepN2ParamId(void* p, CrPsNGroups_t NFA, CrPsNPar_t N2, CrPsParId_t N2ParamId) {
   RepStructHkRep_t* t;
   t = (RepStructHkRep_t*)p;
   t->NFA_[NFA].N2_[N2].N2ParamId = __builtin_bswap16(N2ParamId);
}

/**
 * Get "N" from "RepStructDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNSID_t getHkRepStructDiagCmdN(void* p) {
   RepStructDiagCmd_t* t;
   t = (RepStructDiagCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "RepStructDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setHkRepStructDiagCmdN(void* p, CrPsNSID_t N) {
   RepStructDiagCmd_t* t;
   t = (RepStructDiagCmd_t*)p;
   t->N = N;
}

/**
 * Get pointer to "SID" array from "RepStructDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the SID array.
 */
static inline CrPsSID_t* getHkRepStructDiagCmdSIDArray(void* p) {
   RepStructDiagCmd_t* t;
   t = (RepStructDiagCmd_t*)p;
   return &t->N_[0].SID;
}

/**
 * Get "SID" array from "RepStructDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkRepStructDiagCmdSIDArray(void* p, void* dest) {
   RepStructDiagCmd_t* t;
   t = (RepStructDiagCmd_t*)p;
   memcpy(dest, &t->N_[0].SID, t->N*sizeof(t->N_[0].SID));
}

/**
 * Set "SID" array in "RepStructDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkRepStructDiagCmdSIDArray(void* p, const void* src) {
   RepStructDiagCmd_t* t;
   t = (RepStructDiagCmd_t*)p;
   memcpy(&t->N_[0].SID, src, t->N*sizeof(t->N_[0].SID));
}

/**
 * Get "SID" from "RepStructDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkRepStructDiagCmdSID(void* p, CrPsNSID_t N) {
   RepStructDiagCmd_t* t;
   t = (RepStructDiagCmd_t*)p;
   return __builtin_bswap16(t->N_[N].SID);
}

/**
 * Set "SID" in "RepStructDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkRepStructDiagCmdSID(void* p, CrPsNSID_t N, CrPsSID_t SID) {
   RepStructDiagCmd_t* t;
   t = (RepStructDiagCmd_t*)p;
   t->N_[N].SID = __builtin_bswap16(SID);
}

/**
 * Get "SID" from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkRepStructDiagRepSID(void* p) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return __builtin_bswap16(t->SID);
}

/**
 * Set "SID" in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkRepStructDiagRepSID(void* p, CrPsSID_t SID) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   t->SID = __builtin_bswap16(SID);
}

/**
 * Get "PerGenActionStatus" from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "PerGenActionStatus".
 */
static inline CrPsEnDis_t getHkRepStructDiagRepPerGenActionStatus(void* p) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return t->PerGenActionStatus;
}

/**
 * Set "PerGenActionStatus" in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param PerGenActionStatus Value of "PerGenActionStatus" to be set in
 * packet.
 */
static inline void setHkRepStructDiagRepPerGenActionStatus(void* p, CrPsEnDis_t PerGenActionStatus) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   t->PerGenActionStatus = PerGenActionStatus;
}

/**
 * Get "CollectionInterval" from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "CollectionInterval".
 */
static inline CrPsCollectInterval_t getHkRepStructDiagRepCollectionInterval(void* p) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return __builtin_bswap16(t->CollectionInterval);
}

/**
 * Set "CollectionInterval" in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param CollectionInterval Value of "CollectionInterval" to be set in
 * packet.
 */
static inline void setHkRepStructDiagRepCollectionInterval(void* p, CrPsCollectInterval_t CollectionInterval) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   t->CollectionInterval = __builtin_bswap16(CollectionInterval);
}

/**
 * Get "N1" from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "N1".
 */
static inline CrPsNPar_t getHkRepStructDiagRepN1(void* p) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return t->N1;
}

/**
 * Set "N1" in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param N1 Value of "N1" to be set in packet.
 */
static inline void setHkRepStructDiagRepN1(void* p, CrPsNPar_t N1) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   t->N1 = N1;
}

/**
 * Get pointer to "N1ParamId" array from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the N1ParamId array.
 */
static inline CrPsParId_t* getHkRepStructDiagRepN1ParamIdArray(void* p) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return &t->N1_[0].N1ParamId;
}

/**
 * Get "N1ParamId" array from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkRepStructDiagRepN1ParamIdArray(void* p, void* dest) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   memcpy(dest, &t->N1_[0].N1ParamId, t->N1*sizeof(t->N1_[0].N1ParamId));
}

/**
 * Set "N1ParamId" array in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkRepStructDiagRepN1ParamIdArray(void* p, const void* src) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   memcpy(&t->N1_[0].N1ParamId, src, t->N1*sizeof(t->N1_[0].N1ParamId));
}

/**
 * Get "N1ParamId" from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param N1
 * @return Value of "N1ParamId".
 */
static inline CrPsParId_t getHkRepStructDiagRepN1ParamId(void* p, CrPsNPar_t N1) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return __builtin_bswap16(t->N1_[N1].N1ParamId);
}

/**
 * Set "N1ParamId" in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param N1
 * @param N1ParamId Value of "N1ParamId" to be set in packet.
 */
static inline void setHkRepStructDiagRepN1ParamId(void* p, CrPsNPar_t N1, CrPsParId_t N1ParamId) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   t->N1_[N1].N1ParamId = __builtin_bswap16(N1ParamId);
}

/**
 * Get "NFA" from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "NFA".
 */
static inline CrPsNGroups_t getHkRepStructDiagRepNFA(void* p) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return t->NFA;
}

/**
 * Set "NFA" in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param NFA Value of "NFA" to be set in packet.
 */
static inline void setHkRepStructDiagRepNFA(void* p, CrPsNGroups_t NFA) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   t->NFA = NFA;
}

/**
 * Get "SCSampleRepNum" from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @return Value of "SCSampleRepNum".
 */
static inline CrPsRepNum_t getHkRepStructDiagRepSCSampleRepNum(void* p, CrPsNGroups_t NFA) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return t->NFA_[NFA].SCSampleRepNum;
}

/**
 * Set "SCSampleRepNum" in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param SCSampleRepNum Value of "SCSampleRepNum" to be set in packet.
 */
static inline void setHkRepStructDiagRepSCSampleRepNum(void* p, CrPsNGroups_t NFA, CrPsRepNum_t SCSampleRepNum) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   t->NFA_[NFA].SCSampleRepNum = SCSampleRepNum;
}

/**
 * Get "N2" from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "N2".
 */
static inline CrPsNPar_t getHkRepStructDiagRepN2(void* p) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return t->NFA_[NFA].N2;
}

/**
 * Set "N2" in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param N2 Value of "N2" to be set in packet.
 */
static inline void setHkRepStructDiagRepN2(void* p, CrPsNPar_t N2) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   t->NFA_[NFA].N2 = N2;
}

/**
 * Get pointer to "N2ParamId" array from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @return Pointer to the start of the N2ParamId array.
 */
static inline CrPsParId_t* getHkRepStructDiagRepN2ParamIdArray(void* p, CrPsNGroups_t NFA) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return &t->NFA_[NFA].N2_[0].N2ParamId;
}

/**
 * Get "N2ParamId" array from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkRepStructDiagRepN2ParamIdArray(void* p, CrPsNGroups_t NFA, void* dest) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   memcpy(dest, &t->NFA_[NFA].N2_[0].N2ParamId, t->NFA_[NFA].N2*sizeof(t->NFA_[NFA].N2_[0].N2ParamId));
}

/**
 * Set "N2ParamId" array in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkRepStructDiagRepN2ParamIdArray(void* p, CrPsNGroups_t NFA, const void* src) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   memcpy(&t->NFA_[NFA].N2_[0].N2ParamId, src, t->NFA_[NFA].N2*sizeof(t->NFA_[NFA].N2_[0].N2ParamId));
}

/**
 * Get "N2ParamId" from "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param N2
 * @return Value of "N2ParamId".
 */
static inline CrPsParId_t getHkRepStructDiagRepN2ParamId(void* p, CrPsNGroups_t NFA, CrPsNPar_t N2) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   return __builtin_bswap16(t->NFA_[NFA].N2_[N2].N2ParamId);
}

/**
 * Set "N2ParamId" in "RepStructDiagRep" packet.
 * @param p Pointer to the packet.
 * @param NFA
 * @param N2
 * @param N2ParamId Value of "N2ParamId" to be set in packet.
 */
static inline void setHkRepStructDiagRepN2ParamId(void* p, CrPsNGroups_t NFA, CrPsNPar_t N2, CrPsParId_t N2ParamId) {
   RepStructDiagRep_t* t;
   t = (RepStructDiagRep_t*)p;
   t->NFA_[NFA].N2_[N2].N2ParamId = __builtin_bswap16(N2ParamId);
}

/**
 * Get "SID" from "Rep" packet.
 * @param p Pointer to the packet.
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkRepSID(void* p) {
   Rep_t* t;
   t = (Rep_t*)p;
   return __builtin_bswap16(t->SID);
}

/**
 * Set "SID" in "Rep" packet.
 * @param p Pointer to the packet.
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkRepSID(void* p, CrPsSID_t SID) {
   Rep_t* t;
   t = (Rep_t*)p;
   t->SID = __builtin_bswap16(SID);
}

/**
 * Get "SID" from "DiagRep" packet.
 * @param p Pointer to the packet.
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkDiagRepSID(void* p) {
   DiagRep_t* t;
   t = (DiagRep_t*)p;
   return __builtin_bswap16(t->SID);
}

/**
 * Set "SID" in "DiagRep" packet.
 * @param p Pointer to the packet.
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkDiagRepSID(void* p, CrPsSID_t SID) {
   DiagRep_t* t;
   t = (DiagRep_t*)p;
   t->SID = __builtin_bswap16(SID);
}

/**
 * Get "N" from "OneShotHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNSID_t getHkOneShotHkCmdN(void* p) {
   OneShotHkCmd_t* t;
   t = (OneShotHkCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "OneShotHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setHkOneShotHkCmdN(void* p, CrPsNSID_t N) {
   OneShotHkCmd_t* t;
   t = (OneShotHkCmd_t*)p;
   t->N = N;
}

/**
 * Get pointer to "SID" array from "OneShotHkCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the SID array.
 */
static inline CrPsSID_t* getHkOneShotHkCmdSIDArray(void* p) {
   OneShotHkCmd_t* t;
   t = (OneShotHkCmd_t*)p;
   return &t->N_[0].SID;
}

/**
 * Get "SID" array from "OneShotHkCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkOneShotHkCmdSIDArray(void* p, void* dest) {
   OneShotHkCmd_t* t;
   t = (OneShotHkCmd_t*)p;
   memcpy(dest, &t->N_[0].SID, t->N*sizeof(t->N_[0].SID));
}

/**
 * Set "SID" array in "OneShotHkCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkOneShotHkCmdSIDArray(void* p, const void* src) {
   OneShotHkCmd_t* t;
   t = (OneShotHkCmd_t*)p;
   memcpy(&t->N_[0].SID, src, t->N*sizeof(t->N_[0].SID));
}

/**
 * Get "SID" from "OneShotHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkOneShotHkCmdSID(void* p, CrPsNSID_t N) {
   OneShotHkCmd_t* t;
   t = (OneShotHkCmd_t*)p;
   return __builtin_bswap16(t->N_[N].SID);
}

/**
 * Set "SID" in "OneShotHkCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkOneShotHkCmdSID(void* p, CrPsNSID_t N, CrPsSID_t SID) {
   OneShotHkCmd_t* t;
   t = (OneShotHkCmd_t*)p;
   t->N_[N].SID = __builtin_bswap16(SID);
}

/**
 * Get "N" from "OneShotDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Value of "N".
 */
static inline CrPsNSID_t getHkOneShotDiagCmdN(void* p) {
   OneShotDiagCmd_t* t;
   t = (OneShotDiagCmd_t*)p;
   return t->N;
}

/**
 * Set "N" in "OneShotDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N Value of "N" to be set in packet.
 */
static inline void setHkOneShotDiagCmdN(void* p, CrPsNSID_t N) {
   OneShotDiagCmd_t* t;
   t = (OneShotDiagCmd_t*)p;
   t->N = N;
}

/**
 * Get pointer to "SID" array from "OneShotDiagCmd" packet.
 * @param p Pointer to the packet.
 * @return Pointer to the start of the SID array.
 */
static inline CrPsSID_t* getHkOneShotDiagCmdSIDArray(void* p) {
   OneShotDiagCmd_t* t;
   t = (OneShotDiagCmd_t*)p;
   return &t->N_[0].SID;
}

/**
 * Get "SID" array from "OneShotDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param dest Pointer to memory location where array data are copied to.
 */
static inline void readHkOneShotDiagCmdSIDArray(void* p, void* dest) {
   OneShotDiagCmd_t* t;
   t = (OneShotDiagCmd_t*)p;
   memcpy(dest, &t->N_[0].SID, t->N*sizeof(t->N_[0].SID));
}

/**
 * Set "SID" array in "OneShotDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param src Pointer to memory location from where array data are copied.
 */
static inline void writeHkOneShotDiagCmdSIDArray(void* p, const void* src) {
   OneShotDiagCmd_t* t;
   t = (OneShotDiagCmd_t*)p;
   memcpy(&t->N_[0].SID, src, t->N*sizeof(t->N_[0].SID));
}

/**
 * Get "SID" from "OneShotDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @return Value of "SID".
 */
static inline CrPsSID_t getHkOneShotDiagCmdSID(void* p, CrPsNSID_t N) {
   OneShotDiagCmd_t* t;
   t = (OneShotDiagCmd_t*)p;
   return __builtin_bswap16(t->N_[N].SID);
}

/**
 * Set "SID" in "OneShotDiagCmd" packet.
 * @param p Pointer to the packet.
 * @param N
 * @param SID Value of "SID" to be set in packet.
 */
static inline void setHkOneShotDiagCmdSID(void* p, CrPsNSID_t N, CrPsSID_t SID) {
   OneShotDiagCmd_t* t;
   t = (OneShotDiagCmd_t*)p;
   t->N_[N].SID = __builtin_bswap16(SID);
}


/*----------------------------------------------------------------------------*/
#endif /* CRPSPCKTHK_H */
